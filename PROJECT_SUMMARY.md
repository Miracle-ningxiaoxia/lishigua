# ECHOES | 拾光纪

一个具有电影级视觉体验的友谊纪念网站。

## 🎬 项目概述

这是一个充满情感、质感和仪式感的数字避风港，使用 Next.js 15 + Framer Motion 打造。

## 🎨 技术栈

- **框架**: Next.js 15 (App Router, React Server Components)
- **样式**: Tailwind CSS 4
- **动画**: Framer Motion 12
- **图标**: Lucide React
- **平滑滚动**: Lenis
- **字体**: Geist Sans, Geist Mono, Dancing Script

## 📦 核心模块

### 1. Hero Section (序幕)
**路径**: `src/components/hero/`

**特性**:
- 全屏动态背景视频
- 极简文字加载序列（"有些瞬间，注定成为永恒"）
- Iris Wipe（光圈擦除）转场效果
- 品牌名 "ECHOES | 拾光纪" 带呼吸灯效果
- Infinite Moments 计数器（Spring 物理动画）
- 滚动视差效果（背景缩放 + 模糊）

**关键文件**:
- `Hero.tsx` - 主组件
- `LoadingScreen.tsx` - 加载序列
- `InfiniteMoments.tsx` - 计数器

### 2. Timeline (故事线)
**路径**: `src/components/timeline/`

**特性**:
- 垂直时光轴，中央进度线（Spring 动画）
- 时间节点卡片（玻璃拟态）
- Sticky 年份标签
- 滚动联动进场动画
- 超大半透明年份背景数字

**关键文件**:
- `Timeline.tsx` - 主组件
- `TimelineItem.tsx` - 单个节点

### 3. Gallery (画廊)
**路径**: `src/components/gallery/`

**特性**:
- 3D 拖拽轮播（perspective 1200px）
- 动态 3D 倾斜效果（rotateY ±25deg）
- Lightbox 全屏查看
- 共享元素过渡（layoutId）
- 缩略图 / 高清图分离加载

**关键文件**:
- `Gallery.tsx` - 轮播主组件
- `Lightbox.tsx` - 全屏查看

### 4. The Crew (成员卡片)
**路径**: `src/components/crew/`

**特性**:
- 磁吸倾斜效果（基于鼠标位置）
- 聚光灯交互（聚焦 / 虚化）
- 独特的色彩氛围（每人一个代表色）
- 呼吸灯头像边框
- 悬停弹出个人语录
- 交错入场动画

**关键文件**:
- `Crew.tsx` - 主组件
- `CrewCard.tsx` - 单个卡片

### 5. Future Letter (时光胶囊)
**路径**: `src/components/future/`

**特性**:
- 动态星空背景
- 浮动信封动画
- 信笺展开（共享元素过渡）
- 手写体正文逐行淡入
- 写信表单 + 光点收缩动画
- 光标显示 "Open" 文字

**关键文件**:
- `FutureLetter.tsx` - 主组件
- `LetterEnvelope.tsx` - 信封
- `LetterView.tsx` - 信笺展开
- `WriteLetterForm.tsx` - 写信表单
- `Starfield.tsx` - 星空背景

## 🎮 全局交互组件

### Custom Cursor (自定义光标)
**路径**: `src/components/ui/CustomCursor.tsx`

**特性**:
- 物理弹性跟随（Spring physics）
- 智能识别交互元素
- 悬停放大 + 高斯模糊
- 特殊状态文字显示（如 "Open"）
- 移动端自动隐藏

### Music Player (音乐播放器)
**路径**: `src/components/ui/MusicPlayer.tsx`

**特性**:
- 右下角极简圆形按钮
- 动态频率条动画
- 自动播放（淡入 2 秒）
- Web Audio API 淡入淡出（1.5 秒）
- 悬停显示曲目信息

### Smooth Scroll (平滑滚动)
**路径**: `src/components/SmoothScroll.tsx`

**特性**:
- 全局 Lenis 集成
- 高阻尼物理滚动
- 与 Framer Motion 兼容

### Noise Overlay (噪点层)
**路径**: `src/components/ui/Noise.tsx`

**特性**:
- SVG 噪点滤镜
- 极低透明度（0.03）
- 增强胶片质感

## 🎨 设计系统

### 色彩
- **主色**: 纯黑 #000000
- **文字**: 暖白 #ededed
- **玻璃**: white/[0.02-0.1] + backdrop-blur
- **边框**: white/10-20

### 字体
- **标题**: Geist Sans (font-sans)
- **等宽**: Geist Mono (font-mono)
- **手写**: Dancing Script (font-hand)

### 动画原则
- **入场**: 0.6-0.8s, easeOut
- **交互**: 0.3-0.4s, easeOut
- **Spring**: stiffness 100-500, damping 20-30

## 📁 项目结构

```
src/
├── app/
│   ├── layout.tsx          # 全局布局 + 字体
│   ├── page.tsx            # 首页（整合所有模块）
│   └── globals.css         # 全局样式
├── components/
│   ├── hero/               # Hero Section
│   ├── timeline/           # Timeline
│   ├── gallery/            # Gallery
│   ├── crew/               # The Crew
│   ├── future/             # Future Letter
│   ├── ui/                 # 全局交互组件
│   └── SmoothScroll.tsx    # 平滑滚动
public/
├── audio/
│   └── ambient-bgm.mp3     # 背景音乐
├── images/
│   ├── gallery/            # 画廊图片
│   └── crew/               # 成员头像
└── hero-bg.mp4             # Hero 背景视频
```

## 🚀 快速开始

### 安装依赖
```bash
npm install
```

### 开发模式
```bash
npm run dev
```

访问 `http://localhost:3000`

### 构建生产版本
```bash
npm run build
npm start
```

## 📝 素材准备清单

### 必需素材
- [ ] Hero 背景视频：`public/hero-bg.mp4`
- [ ] 背景音乐：`public/audio/ambient-bgm.mp3`

### 可选素材
- [ ] Timeline 照片（Timeline.tsx 中配置）
- [ ] Gallery 图片：
  - 缩略图：`public/images/gallery/thumb-{1-6}.jpg`
  - 高清图：`public/images/gallery/full-{1-6}.jpg`
- [ ] Crew 头像：`public/images/crew/{name}.jpg`

## 🎯 性能优化

### 已实现
- ✅ 图片懒加载
- ✅ 缩略图 / 高清图分离
- ✅ will-change: transform
- ✅ GPU 加速（transform, filter）
- ✅ Spring 物理动画（避免重渲染）
- ✅ AnimatePresence 优化

### 建议
- 使用 Cloudinary 托管媒体资源
- 启用 Next.js Image Optimization
- 压缩视频和音频文件

## 🎨 自定义指南

### 修改配色
在 `globals.css` 中修改：
```css
:root {
  --background: #000000;
  --foreground: #ededed;
}
```

### 添加新模块
1. 在 `src/components/` 创建新文件夹
2. 实现组件
3. 在 `page.tsx` 中导入

### 修改动画
调整 Framer Motion 的 `transition` 参数：
- `duration`: 持续时间
- `delay`: 延迟时间
- `ease`: 缓动函数
- `stiffness`/`damping`: Spring 参数

## 🐛 常见问题

### 1. 控制台警告：scroll offset
**解决**: 已通过使用 window scroll 代替 target scroll 修复

### 2. 音乐无法自动播放
**原因**: 浏览器自动播放策略
**解决**: 点击播放器手动启动

### 3. 自定义光标不显示
**原因**: 移动端或触摸屏
**解决**: 仅在桌面端（hover: hover）显示

## 📄 License

MIT

## 🙏 致谢

感谢所有参与这段友谊旅程的朋友们。

---

**Created with ❤️ by [Your Name]**
