# 众声（The Crew）模块

情侣合体交互系统 - 让友谊纪念馆更有温度 💕

---

## 🚀 快速开始

### 1. 添加情侣合照

在 `public/images/crew/couples/` 目录下放入 7 张情侣合照：

```
couple-1.jpg  # 陈果子 & 范大爷
couple-2.jpg  # 范小车 & 蓉姐
couple-3.jpg  # 李果子 & 王睿娃
couple-4.jpg  # 小霞 & 邱雪晶
couple-5.jpg  # 王燕 & 舒兴友
couple-6.jpg  # 唐蛋 & 王老师
couple-7.jpg  # 佳姐 & 袁老师
```

### 2. 更新数据配置

编辑 `crewData.ts`，填充 `coupleImage` 字段：

```typescript
export const couplesData: Couple[] = [
  {
    id: 'couple-1',
    partner1Id: 'crew-1',
    partner2Id: 'crew-2',
    coupleImage: '/images/crew/couples/couple-1.jpg', // 👈 填这里
    declaration: '你们的专属宣言',
    accentColor: '#f43f5e',
  },
  // ... 其他 6 对
]
```

### 3. 运行项目

```bash
npm run dev
```

访问 "众声" 模块，点击任意两位成员，看看会发生什么！✨

---

## 📂 文件结构

```
crew/
├── Crew.tsx              # 主组件（交互逻辑）
├── CrewCard.tsx          # 成员卡片组件
├── CoupleCard.tsx        # 情侣合体卡片组件
├── crewData.ts           # 数据定义（14成员 + 7情侣）
├── COUPLE_MERGE_GUIDE.md # 详细指南
└── README.md             # 本文件
```

---

## 🎮 交互说明

1. **点击第一位成员** → 卡片发光，右上角显示 ✓
2. **点击第二位成员（配偶）** → 触发合体动画
3. **查看情侣合照和宣言** → 全屏展示
4. **点击任意处或 X 按钮** → 关闭返回

---

## 🎨 自定义

### 修改宣言

在 `crewData.ts` 的 `couplesData` 中修改 `declaration` 字段。

### 修改代表色

在 `crewData.ts` 的 `couplesData` 中修改 `accentColor` 字段。

### 添加新情侣

1. 在 `crewMembers` 中添加两位成员
2. 设置彼此的 `partnerId`
3. 在 `couplesData` 中添加情侣数据

---

## 📖 详细文档

查看 [COUPLE_MERGE_GUIDE.md](./COUPLE_MERGE_GUIDE.md) 获取完整指南。

---

**Made with ❤️ for your friendship memories**
